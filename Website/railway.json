{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "flyio-deployment/Dockerfile"
  },
  "deploy": {
    "startCommand": "python hybrid_router.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
