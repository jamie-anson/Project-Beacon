{
  "constraints": "(& (golem.inf.mem.gib>=8) (golem.inf.cpu.threads>=4) (golem.inf.storage.gib>=20) (golem.runtime.name=\"vm\") ${GPU_VENDOR_EXPR} ${GPU_VRAM_EXPR})",
  "x_notes": {
    "tier": "NVIDIA GPU — 8+ GiB VRAM",
    "placeholders": {
      "GPU_VENDOR_EXPR": "Replace with expression matching NVIDIA vendor (e.g., equality check on a GPU vendor property)",
      "GPU_VRAM_EXPR": "Replace with expression asserting >= 8 GiB VRAM"
    },
    "example": "${GPU_VENDOR_EXPR} might be something like (golem.inf.gpu.vendor=\"nvidia\") if your runtime exposes such a property (validate exact key)."
  }
}
